import { expect } from "chai";
import "mocha";

describe("client-shared should work", async function () {
  it("client-shared should do something", function () {
    expect(true).to.eq(true);
  });
});
