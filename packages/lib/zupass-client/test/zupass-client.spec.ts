import { expect } from "chai";
import "mocha";

describe("zupass-client should work", async function () {
  it("zupass-client should do something", function () {
    expect(true).to.eq(true);
  });
});
