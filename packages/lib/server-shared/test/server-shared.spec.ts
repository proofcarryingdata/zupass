import { expect } from "chai";
import "mocha";

describe("server-shared should work", async function () {
  it("server-shared should do something", function () {
    expect(true).to.eq(true);
  });
});
