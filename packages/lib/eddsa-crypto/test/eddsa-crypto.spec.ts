import { expect } from "chai";
import "mocha";

describe("eddsa-crypto should work", async function () {
  it("eddsa-crypto should do something", function () {
    expect(true).to.eq(true);
  });
});
