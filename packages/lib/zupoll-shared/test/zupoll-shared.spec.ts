import { expect } from "chai";
import "mocha";

describe("zupoll-shared should work", async function () {
  it("zupoll-shared should do something", function () {
    expect(true).to.eq(true);
  });
});
