{
  "extends": ["//"],
  "pipeline": {
    "gen-test-artifacts": {
      "dependsOn": [],
      "inputs": [
        "$TURBO_DEFAULT$",
        "scripts/gen-test-artifacts.ts",
        "circuits/**",
        "circomkit.json",
        "circuits.json"
      ],
      "outputs": ["artifacts/**"],
      "outputMode": "new-only",
      "cache": true
    },
    "build": {
      "dependsOn": ["^build", "gen-test-artifacts"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "src/**",
        "circuits/**",
        "artifacts/**",
        "test/**",
        "circomkit.json",
        "circuits.json"
      ],
      "outputs": ["dist/**", "*.tsbuildinfo"],
      "cache": true
    }
  }
}
