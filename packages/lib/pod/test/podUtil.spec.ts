import { expect } from "chai";
import "mocha";

describe("podUtil helpers should work", async function () {
  //TODO(artwyman): Test util helpers, including illegal value checking.
  it("podUtil should do something", function () {
    expect(true).to.eq(true);
  });
});
