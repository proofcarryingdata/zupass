import { expect } from "chai";
import "mocha";

describe("semaphore-sig-next should work", async function () {
  it("semaphore-sig-next should do something", function () {
    expect(true).to.eq(true);
  });
});
