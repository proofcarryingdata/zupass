import { expect } from "chai";
import "mocha";

describe("pod-ticket-pcd should work", async function () {
  it("pod-ticket-pcd should do something", function () {
    expect(true).to.eq(true);
  });
});
