{
  "extends": ["//"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "src/**",
        "*.tsconfig.json",
        "rollup.config.ts",
        "tsconfig.json"
      ],
      "outputs": ["dist/**"],
      "cache": true
    }
  }
}
