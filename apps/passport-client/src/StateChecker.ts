import { AppState } from "./state";

export class StateChecker {
  public async checkState(appState: AppState): Promise<boolean> {
    let valid = true;

    if (valid) {
      //
    } else {
      //
    }

    return valid;
  }
}
