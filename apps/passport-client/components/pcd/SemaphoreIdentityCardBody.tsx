import { SemaphoreIdentityPCD } from "@pcd/semaphore-identity-pcd";
import React from "react";

export function SemaphoreIdentityCardBody({
  pcd,
}: {
  pcd: SemaphoreIdentityPCD;
}) {
  return <div>semaphore identity</div>;
}
