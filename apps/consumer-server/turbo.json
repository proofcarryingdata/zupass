{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": [".env", "$TURBO_DEFAULT$", "src/**", "test/**"],
      "outputs": ["*.tsbuildinfo"],
      "cache": true
    }
  }
}
