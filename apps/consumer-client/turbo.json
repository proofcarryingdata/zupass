{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": [".env", "$TURBO_DEFAULT$", "public/index.html", "src/**", "build.ts"],
      "outputs": ["public/js/**"],
      "cache": true
    }
  }
}
