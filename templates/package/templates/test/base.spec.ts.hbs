import { expect } from "chai";
import "mocha";

describe("{{kebabCase name}} should work", async function () {
  it("{{kebabCase name}} should do something", function () {
    expect(true).to.eq(true);
  });
});
